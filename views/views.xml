<odoo>
  <data>
    <record id="view_move_form_grn_reference" model="ir.ui.view">
      <field name="name">account.move.form.grn.reference</field>
      <field name="model">account.move</field>
      <field name="inherit_id" ref="account.view_move_form"/>
      <field name="arch" type="xml">
        <field name="partner_shipping_id" position="after">
          <field name="grn_reference"
                 placeholder="GRN Reference"
                 invisible="move_type not in ('out_invoice', 'out_refund')"
                 readonly="state != 'draft'"/>
        </field>
      </field>
    </record>

    <record id="view_account_invoice_filter_grn_reference" model="ir.ui.view">
      <field name="name">account.move.invoice.select.grn.reference</field>
      <field name="model">account.move</field>
      <field name="inherit_id" ref="account.view_account_invoice_filter"/>
      <field name="arch" type="xml">
        <field name="name" position="replace">
          <field name="name" string="Invoice"
                 filter_domain="[
                     '|', '|', '|', '|', '|',
                     ('name', 'ilike', self), ('invoice_origin', 'ilike', self),
                     ('ref', 'ilike', self), ('payment_reference', 'ilike', self),
                     ('grn_reference', 'ilike', self),
                     ('partner_id', 'child_of', self)]"
                     context="{'active_test': False}"/>
        </field>
        <field name="name" position="after">
          <field name="grn_reference"/>
        </field>
      </field>
    </record>
  </data>
</odoo>
